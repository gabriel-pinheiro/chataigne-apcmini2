{
  "name": "chataigne-apcmini2",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "engines": {
    "node": ">=20"
  },
  "scripts": {
    "generate": "tsx scripts/generate-module.ts",
    "typecheck": "tsc --noEmit",
    "test:unit": "tsx --test scripts/test/*.test.ts",
    "test": "npm run typecheck && npm run test:unit"
  },
  "devDependencies": {
    "@types/node": "^26.4.1",
    "tsx": "^4.23.13",
    "typescript": "^7.0.2"
  }
}
